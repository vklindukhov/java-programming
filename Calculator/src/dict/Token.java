/**
 * Lists the types of tokens that the Tokeniser can create
 * @author Max Fisher
 * @version 2.0
 */
package dict;

public enum Token {
	L_BRACKET, R_BRACKET, COMMA, UN_OPERATOR, BIN_OPERATOR, NUMBER, CONSTANT, SUBLIST, OPERATION
}
